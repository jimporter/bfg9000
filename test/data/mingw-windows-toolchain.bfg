# -*- python -*-

target_platform('windows')

environ['CXX'] = which(['x86_64-w64-mingw32-g++-win32',
                        'x86_64-w64-mingw32-g++'])
environ['LDFLAGS'] = '-static-libstdc++ -static-libgcc'

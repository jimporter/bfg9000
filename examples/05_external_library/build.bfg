# -*- python -*-

# Most significant projects require the use of external libraries. Currently,
# you can reference an external library by passing its name as a string to the
# `libs` argument for a build. NOTE: This will change soon, so be ready to
# update any build.bfg files that rely on this!

prog = executable('program', files=['program.cpp'], libs=['z'])

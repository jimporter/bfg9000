# -*- python -*-

include = header_directory('include')
lib = shared_library('sub/library', files=['src/library.cpp'],
                     include=[include])
prog = executable('sub/program', files=['src/program.cpp'], include=[include],
                  libs=[lib])
install(prog, lib, include)

#!/usr/bin/env python
# -*- python -*-

import os.path
import sys

import builtins
import rule
import builtin_rules
import backends.make as backend
import utils

builddir = os.path.abspath(sys.argv[1])
bfgfile = 'build.bfg'

execfile(os.path.join(builddir, bfgfile), builtins.all_builtins)
backend.write(builddir, rule.all_targets)
